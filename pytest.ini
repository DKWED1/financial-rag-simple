# pytest.ini
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 忽略警告
filterwarnings =
    ignore::UserWarning
    ignore::DeprecationWarning

# 输出详细日志
addopts = -v --tb=short

# 标记
markers =
    slow: 运行较慢的测试
    integration: 集成测试
    unit: 单元测试
