version: "3.8"
secrets:
  dashscope_key:
    file: ./secrets/dashscope_api_key.txt

services:
  rag-api:
    build: .
    ports:
      - "8000:8000"
    secrets:
      - dashscope_key
    environment:
      - DASHSCOPE_API_KEY_FILE=/run/secrets/dashscope_key